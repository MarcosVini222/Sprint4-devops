trigger:
  branches:
    include:
      - main
      - master    # pipeline dispara automaticamente em push para main/master

pool:
  vmImage: 'ubuntu-latest'   # ambiente Linux no agente hospedado da Microsoft

variables:
  # =========================
  # 🔧 VARIÁVEIS DE CONFIGURAÇÃO
  # =========================
  appName: 'motoblu556496'             # nome da aplicação
  acrName: 'acrrm556496'               # nome do ACR criado no Azure
  acrLoginServer: 'acrrm556496.azurecr.io'  # endpoint do ACR (obtido via az acr show)
  imageRepository: 'fiap/$(appName)'   # nome do repositório dentro do ACR
  dockerfilePath: '**/Dockerfile'      # caminho do Dockerfile
  tag: '$(Build.BuildNumber)'          # número do build como tag da imagem
  resourceGroup: 'rg-fiap556496'       # grupo de recursos no Azure
  containerGroup: 'aci-motoblu'        # nome do Container Instance
  location: 'brazilsouth'              # região do Azure

# ===============================================================
# 🏗️ STAGE 1 - BUILD & PUSH (Docker → Azure Container Registry)
# ===============================================================
stages:
  - stage: Build
    displayName: 'Build and push image to ACR'
    jobs:
      - job: BuildAndPush
        displayName: 'Docker build and push to ACR'
        steps:
          - task: Docker@2
            displayName: '🔨 Build and Push Docker Image'
            inputs:
              # 🔑 Service Connection do tipo "Docker Registry"
              # Deve apontar para seu ACR (SC-FIAP-556496)
              containerRegistry: 'SC-FIAP-556496'
              repository: '$(imageRepository)'
              command: 'buildAndPush'
              Dockerfile: '$(dockerfilePath)'
              tags: |
                $(tag)

  # ===============================================================
  # ☁️ STAGE 2 - DEPLOY (Azure Container Instance)
  # ===============================================================
  - stage: Deploy
    displayName: 'Deploy image to Azure Container Instance (ACI)'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - job: DeployToACI
        displayName: 'Deploy container to Azure ACI'
        steps:
          - task: AzureCLI@2
            displayName: '🚀 Deploy to Azure ACI'
            inputs:
              # 🔑 Service Connection do tipo "Azure Resource Manager"
              azureSubscription: 'SC-AZURE-RM-556496'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                echo "🔹 Iniciando deploy da aplicação no Azure Container Instance..."
                
                # Deleta o container antigo (caso já exista)
                az container delete \
                  --resource-group $(resourceGroup) \
                  --name $(containerGroup) \
                  --yes || true
                
                echo "🔹 Criando novo container com a imagem atualizada..."
                az container create \
                  --resource-group $(resourceGroup) \
                  --name $(containerGroup) \
                  --image $(acrLoginServer)/$(imageRepository):$(tag) \
                  --registry-login-server $(acrLoginServer) \
                  --dns-name-label $(appName) \
                  --restart-policy Always \
                  --ports 8080 \
                  --location $(location)
                
                echo "✅ Deploy concluído!